---
topic: Multi-Agent Model Delegation System
updated: 2026-06-27T18:03
---

- (capability) CAP-1: Single agent task execution — user can run any registered agent with an input payload and get a task ID back
- (capability) CAP-2: Multi-step DAG workflow — user can define dependent task steps as a YAML DAG; orchestrator executes in topological order
- (capability) CAP-3: Sub-delegation — an agent can delegate sub-tasks to other agents via agent/delegate tool; orchestrator validates and routes
- (capability) CAP-4: Mirror audit — agent outputs can be validated by a designated mirror agent before proceeding
- (capability) CAP-5: Shared knowledge base — agents communicate through filesystem artifacts at _kb/
- (capability) CAP-6: Dual entry point — same orchestration engine serves IDE (MCP stdio/SSE) and CI/CD (CLI runner)
- (event) SPEC.md created with 6 capabilities, 8 constraints, 6 non-goals, 3 assumptions, 3 open questions
- (event) Self-validate Pass 1 (Coherence): ALL PASS — 6 capabilities pass rules 1-2, 8 constraints bend design, 6 non-goals are explicit, success signal testable, IDs unique, prose lean
- (event) Self-validate Pass 2 (Preservation): ALL PASS — brainstorm-intent claims landed in SPEC.md kernel, architecture decisions covered by ARCHITECTURE-SPINE.md companion, task-list build plan underlies constraints/deferred sections
- (decision) Concurrency: DAG executor runs all ready nodes in parallel, unbounded — no artificial concurrency limit
- (decision) Authentication: SSE transport supports OAuth2 (Bearer token with configurable provider) — not in scope for stdio transport
- (decision) Stack versions: migrate all dependencies to current June 2026 releases — zod ^4.4.3, better-sqlite3 ^12.11.1, js-yaml ^5.1.0, Node 24.x, TypeScript 5.7+, uuid 11.x, @modelcontextprotocol/sdk latest stable
- (event) All 3 open questions resolved by user direction
