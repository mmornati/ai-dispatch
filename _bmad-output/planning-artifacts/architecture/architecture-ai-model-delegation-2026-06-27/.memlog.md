---
scope: Multi-Agent Model Delegation System — MCP orchestrator, DAG engine, agent configs, KB, mirror protocol, CI/CD entry point
purpose: Architecture spine to guide implementation of the delegation system
altitude: feature
updated: 2026-06-27T18:06
---

- (question) Purpose and audience for the architecture spine — asked user whether spine alone suffices or if additional artifacts needed
- (decision) Paradigm: Orchestrator-based task routing — central MCP server dispatches to config-defined agents, manages DAG workflow graph
- (decision) Stack: Node.js 22 + TypeScript + @modelcontextprotocol/sdk + Zod + better-sqlite3 + js-yaml — confirmed as-is
- (decision) Entry points: dual (MCP client + CI/CD), same DAG engine underneath — confirmed
- (decision) Agent config format: .agent.md with YAML frontmatter, file-based discovery in agents/ dir — confirmed
- (decision) DAG engine: config-driven topological sort (no LLM in planning loop) — confirmed for MVP, room to add LLM-assisted decomposition later via optional TaskAnalyzer step
- (direction) KB-based agent communication preferred over inline context passing — filesystem persistence provides resilience, audit trail, and crash recovery
- (decision) Mirror protocol: sequential (primary → mirror → retry) — confirmed
- (decision) Deployment model: local-first (stdio to IDE), SSE server available for Copilot Desktop App when needed — confirmed
- (event) spine finalized with reviewer fixes applied
- (decision) AD-9 (DAG concurrency): DAG executor runs all ready nodes in parallel, unbounded — no artificial concurrency limit. Removed from Deferred.
- (decision) AD-10 (SSE auth): SSE transport supports OAuth2 with configurable JWKS URI or symmetric secret key via jose library. Stdio transport has no auth. Removed from Deferred.
- (decision) AD-8 updated: Stack migrated to latest — Node 24, TS 6.0.3, zod ^4.4.3, better-sqlite3 ^12.11.1, js-yaml ^5.2.0, uuid ^14.0.1, jose ^6.2.3
